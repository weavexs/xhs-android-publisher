# Changelog

## 0.6.0 - 2026-08-09

- 将学习范围扩展到所有手机操作和界面，并增加确定性操作的被动经验记录器。
- 发布、预约、删除、隐私、登录、验证码、支付和账号策略页面只允许识别、定位、提示与结果核验，不扩大模型执行权限。
- 经验记录包含界面指纹、动作序列、结果、文章编号和证据路径，便于后续安全复用与审计。

## 0.50 - 2026-08-07

- 增加 UI-TARS 只读观察器和受控故障恢复层，常规 ADB 找不到文本、资源或普通控件时自动记录事故并尝试一次恢复。
- 恢复动作限定为普通点击、滑动、返回和等待；发布、定时、删除、登录、验证码、支付、隐私及账号安全操作保持硬停止。
- 默认支持持久化 Codex 接管队列，也可配置 UI-TARS 兼容视觉模型；非本地端点必须明确允许上传手机截图。
- 新恢复轨迹连续两次成功后晋级为稳定经验，首次复用失败立即隔离；未处理接管项会阻止新的草稿、定时和发布任务。
- 增加 `observe --no-model` 与 `recovery-status` 命令，以及事故、候选经验、稳定经验和隔离经验记录。
- 按型号与传输方式精确选择 USB OnePlus，忽略其他网络 ADB 设备，避免视觉接管误操作另一台设备。

## 0.4.0 - 2026-08-07

- 打包通用ADB小红书发布操作器和中文输入助手。
- 增加设备、屏幕分辨率、小红书和输入助手预检。
- 使用文章专属临时相册及本机/手机SHA-256核对保持图片顺序。
- 支持草稿、原生定时发布和带发布后验收的公开发布。
- 增加同标题重复保护、失败停止和所有结果后的熄屏验证。
- 将设备PIN迁移到macOS钥匙串，配置样例不包含真实账号与设备资料。
- 增加一键版本发布脚本，自动推送标签并生成本次更新说明。
